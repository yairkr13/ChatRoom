<nav class="navbar navbar-expand-lg navbar-dark bg-success bg-opacity-25">
    <div class="container">
        <a class="navbar-brand fw-semibold text-success" href="/">Chatroom App</a>
        <button
                class="navbar-toggler"
                type="button"
                data-bs-toggle="collapse"
                data-bs-target="#navbarNav"
                aria-controls="navbarNav"
                aria-expanded="false"
                aria-label="Toggle navigation"
        >
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <% if (isAuth) { %>
                    <li class="nav-item">
                        <a class="nav-link <%= page === 'chatroom' ? 'active' : '' %> text-success fw-semibold" href="/chatroom">Chatroom</a>
                    </li>
                <% } %>
            </ul>

            <% if (page === 'chatroom') { %>
                <form class="d-flex me-3" role="search" onsubmit="return false;">
                    <input
                            class="form-control me-2"
                            type="search"
                            placeholder="Search messages"
                            aria-label="Search messages"
                            id="search-input"
                    />
                    <button class="btn btn-outline-success" type="button" id="search-btn">Search</button>
                    <button class="btn btn-outline-success ms-1" type="button" id="clear-search-btn">Clear</button>
                </form>
            <% } %>

            <ul class="navbar-nav mb-2 mb-lg-0">
                <% if (isAuth) { %>
                    <li class="nav-item">
                        <a class="nav-link text-success fw-semibold" href="/logout" id="logout-btn">Logout</a>
                    </li>
                <% } else { %>
                    <li class="nav-item">
                        <a class="nav-link <%= page === 'login' ? 'active' : '' %> text-success fw-semibold" href="/login">Login</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= page === 'register' ? 'active' : '' %> text-success fw-semibold" href="/register">Register</a>
                    </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>
